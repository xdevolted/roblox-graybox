# Core loop

The core loop is intentionally not designed during repository setup. Document one testable loop here
before gameplay implementation: player action, server validation, authoritative state change, client
feedback, completion condition, and reset.

