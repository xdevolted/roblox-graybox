--!strict

local DebugHudController = {}

function DebugHudController.start()
    -- Prototype-only diagnostics belong here when a gameplay slice needs them.
end

return table.freeze(DebugHudController)
