schema_version: 1
name: smoke

tests:
  include:
    - tests/playtests/baseline/**/*.yaml

execution:
  fail_fast: true
  timeout_seconds: 120

evidence:
  screenshots: on_failure
  console: errors_and_warnings

cleanup:
  stop_playtest: true
